# public_server
A public server based on ngrok. By default listens on port `3000` on local machine, which can be changed in `index.js`.

## Launches a ngrok server

1. Url is mentioned in console.
2. Browse http://localhost:3000 to see request history.
